@model IEnumerable<ClienteVM>

<div class="card">
    <div class="card-header">
        <h5>Clientes</h5>
    </div>
    <div class="card-body">
        <a class="btn btn-success btn-sm" asp-action="Upsert" asp-controller="Cliente" asp-route-id="0">Crear Nuevo</a>
        <hr />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Fecha Nac.</th>
                    <th>Cuit</th>
                    <th>Domicilio</th>
                    <th>Celular</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.Apellido</td>
                        <td>@item.FechaDeNacimiento.ToString("dd/MM/yyyy")</td>
                        <td>@item.Cuit</td>
                        <td>@item.Domicilio</td>
                        <td>@item.Celular</td>
                        <td>@item.Email</td>
                        <td>
                            <a class="btn btn-primary btn-sm " asp-action="Upsert" asp-controller="Cliente" asp-route-id="@item.Id"> Editar</a>
                            @* <a class="btn btn-danger btn-sm " asp-action="Eliminar" asp-controller="Cliente" asp-route-id="@item.Id"> Eliminar</a> *@
                        </td>
                    </tr>

                }
            </tbody>
        </table>
    </div>
</div>